apiVersion: v1
kind: Service
metadata:
  name: seerr
  namespace: media
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: seerr
  ports:
    - name: http
      protocol: TCP
      port: 5055
      targetPort: 5055
